# Configure Bitnami OAuth service

To test the Bitnami OAuth login, create a `.env` file in the root of the repository and set the following environment variables:

```
AUTH_CLIENT_ID=
AUTH_CLIENT_SECRET=
```

Then run `docker-compose up` as usual.
